// Create a Boolean value for an ID variable
let isValidID = false;

//  Using a ternary operator, create a message variable that will check
// whether their ID is valid and either allow a person into a venue or not
let message = isValidID ? "ID is valid and entry is allowed" : "ID is invalid and entry is not allowed";

// Output the response to the console
console.log(message)